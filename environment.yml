name: env
channels:
  - defaults
dependencies:
  - python=3.6
  - pytorch
  - tensorflow-gpu[version='<2']
  - hdf5=1.10.4
  - pandas
  - tqdm
  - coloredlogs
prefix: /home/tobias/anaconda3/envs/testenv
